<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tiktok.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.tiktok.user.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="status" column="status"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT id, username, password_hash, status, role, created_at, updated_at
        FROM `user`
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="selectById" resultMap="UserResultMap">
        SELECT id, username, password_hash, status, role, created_at, updated_at
        FROM `user`
        WHERE id = #{id}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.tiktok.user.entity.User">
        INSERT INTO `user` (id, username, password_hash, status, role, created_at, updated_at)
        VALUES (#{id}, #{username}, #{passwordHash}, #{status}, #{role}, #{createdAt}, #{updatedAt})
    </insert>
</mapper>