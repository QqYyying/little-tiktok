# Little TikTok 期末大作业报告

## 一、项目概述

本项目是一个仿 TikTok 的短视频分享平台，采用 Spring Boot + Next.js 技术栈开发，实现了视频推荐、用户互动、视频管理等核心功能。

---

## 二、已完成功能清单

### 2.1 视频推荐主页功能点（20分）

| 功能点 | 分值 | 完成情况 | 说明 |
|--------|------|----------|------|
| 推荐视频（按点赞数最高） | 10分 | ✅ 已完成 | 视频推荐按点赞数降序排列，热门视频优先展示 |
| 访问过不再推荐 | - | ✅ 已完成 | 通过 video_view 表记录用户浏览历史，推荐时过滤已观看视频 |
| 视频上下滑动 | 5分 | ✅ 已完成 | 支持手势滑动切换上下视频，查看上一个/下一个视频 |
| 视频点赞功能 | 5分 | ✅ 已完成 | 支持点赞/取消点赞，实时更新点赞数 |

### 2.2 我的视频管理（15分）

| 功能点 | 分值 | 完成情况 | 说明 |
|--------|------|----------|------|
| 发布视频 | 5分 | ✅ 已完成 | 支持视频和封面上传，表单验证 |
| 查看我的视频（分页） | 5分 | ✅ 已完成 | 分页展示用户上传的视频列表 |
| 删除我的视频（权限控制） | 5分 | ✅ 已完成 | 用户仅能删除自己发布的视频 |

### 2.3 用户系统（5分）

| 功能点 | 分值 | 完成情况 | 说明 |
|--------|------|----------|------|
| 用户登录 | - | ✅ 已完成 | JWT 令牌认证 |
| 用户注册 | - | ✅ 已完成 | 用户名密码注册 |
| 用户注销 | - | ✅ 已完成 | Token 黑名单机制 |

### 2.4 后台其他功能点设计（25分）

| 功能点 | 分值 | 完成情况 | 说明 |
|--------|------|----------|------|
| 数据库设计 | 5分 | ✅ 已完成 | MySQL 数据库，包含用户、视频、点赞、收藏、浏览记录等表 |
| 视频存储设计 | 5分 | ✅ 已完成 | MinIO 对象存储 + 本地文件系统双备份 |
| 日志记录 | 5分 | ✅ 已完成 | AOP 切面记录每个请求的输入输出 |
| 耗时记录 | 5分 | ✅ 已完成 | 记录每个接口的耗时，支持慢请求标记（>500ms） |
| 安全校验 | 5分 | ✅ 已完成 | JWT 认证、权限控制、参数校验 |

---

## 三、超额完成功能

### 3.1 扩展功能

| 功能点 | 说明 |
|--------|------|
| **视频收藏功能** | 仿"我喜欢"功能实现，支持收藏/取消收藏，收藏列表管理 |
| **浏览记录** | 记录用户浏览历史，支持查看浏览记录列表 |
| **视频代理** | 通过后端代理解决 MinIO 跨域问题，支持多种视频格式（mp4、mov） |
| **静音状态保持** | 切换视频时保持静音/非静音状态 |

### 3.2 前端优化

| 优化点 | 说明 |
|--------|------|
| **性能优化** | React.memo 优化组件渲染，视频预加载，图片懒加载 |
| **UI 美化** | 现代化渐变配色（蓝青色系），圆角卡片设计，阴影效果 |
| **登录页面美化** | 渐变背景、动态装饰、Logo、标签切换动画 |
| **响应式设计** | 移动端友好布局 |

### 3.3 后端增强

| 增强点 | 说明 |
|--------|------|
| **MinIO 集成** | 对象存储服务，支持分布式文件存储 |
| **Redis 缓存** | 配置 Redis 支持，可用于缓存热门视频 |
| **API 文档** | OpenAPI/Swagger 文档自动生成 |
| **监控支持** | Prometheus + Grafana 监控配置 |

---

## 四、技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端框架 | Next.js | 14.x |
| 前端语言 | TypeScript | - |
| 样式框架 | Tailwind CSS | 3.x |
| 图标库 | Lucide React | - |
| 后端框架 | Spring Boot | 3.x |
| 后端语言 | Java | 21 |
| 数据库 | MySQL | 8.x |
| 对象存储 | MinIO | - |
| 认证机制 | JWT | - |
| ORM | MyBatis | - |

---

## 五、API设计规范

### 5.1 统一返回格式

本项目所有接口统一使用 `ApiResponse` 作为响应包装，保证前端处理逻辑一致，也便于日志追踪与错误排查。返回结构如下：

```json
{
   "code": "OK",
   "message": "success",
   "data": {},
   "requestId": "req_123456"
}
```

失败时返回：

```json
{
   "code": "PERMISSION_DENIED",
   "message": "无权限删除该视频",
   "data": null,
   "requestId": "req_123456"
}
```

其中 `code` 用于程序判断，`message` 用于展示或排查，`requestId` 用于日志链路追踪。后端已在统一响应封装中固定该结构，避免接口返回杂乱数据格式。

### 5.2 统一错误码

本项目错误码由后端 `ErrorCode` 枚举统一管理，常用错误码包括：

- `OK`：请求成功
- `INVALID_ARGUMENT`：参数错误
- `UNAUTHORIZED`：未登录或登录失效
- `TOKEN_EXPIRED`：登录已过期
- `PERMISSION_DENIED`：无权限操作
- `NOT_FOUND`：资源不存在
- `CONFLICT`：资源冲突
- `INTERNAL_ERROR`：系统异常

业务上按最终状态处理时，可以优先返回成功结果。例如重复点赞不必直接报错，可以返回 `OK`，并在 `data` 中说明当前状态为已点赞，前端据此刷新 UI 即可。

### 5.3 字段命名规范

对外 API 统一使用 `camelCase` 命名，例如 `userId`、`videoId`、`likeCount`、`createdAt`。数据库字段继续使用 `snake_case`，例如 `user_id`、`video_id`、`like_count`、`created_at`，由服务端 DTO 和实体负责转换。

ID 字段统一使用字符串，时间字段统一使用 ISO 字符串，便于阅读和调试。例如：

```json
{
   "videoId": "vid_001",
   "title": "校园生活记录",
   "likeCount": 10,
   "createdAt": "2026-05-20T17:00:00"
}
```

### 5.4 认证方式

需要登录的接口通过请求头传递 Token：

```http
Authorization: Bearer <token>
```

后端通过 `AuthInterceptor` 在请求进入 Controller 前解析 Token，并将当前用户信息写入 `UserContext`。业务层直接从上下文读取当前用户 ID，避免前端伪造 `userId`。

本项目中，发布视频、删除视频、点赞、收藏、我的视频列表等接口都需要认证；部分公开读取接口仍可匿名访问。

---

## 六、项目结构

```
little-tiktok/
├── backend/                    # 后端服务
│   ├── src/main/java/com/tiktok/
│   │   ├── recommend/          # 推荐模块
│   │   ├── video/              # 视频模块
│   │   ├── user/               # 用户模块
│   │   ├── like/               # 点赞模块
│   │   ├── favorite/           # 收藏模块
│   │   ├── view/               # 浏览记录模块
│   │   ├── log/                # 日志模块
│   │   └── common/             # 公共模块
│   └── src/main/resources/
│       ├── mapper/             # MyBatis 映射文件
│       └── application.yml     # 配置文件
├── frontend/                   # 前端应用
│   ├── app/                    # 页面路由
│   ├── src/components/         # 组件
│   ├── src/api/                # API 接口
│   └── src/hooks/              # 自定义 hooks
└── deploy/                     # 部署配置
    ├── prometheus/             # Prometheus 配置
    └── grafana/                # Grafana 配置
```

---

## 七、运行说明

### 7.1 环境要求

- Java 21+
- Node.js 20+
- MySQL 8.0+
- MinIO（可选，用于视频存储）

### 7.2 启动步骤

1. **启动 MySQL**，创建数据库并执行 `table_v3.sql`

2. **启动 MinIO**（可选）：
   ```bash
   minio server /path/to/minio/data
   ```

3. **启动后端**：
   ```bash
   cd backend
   mvn spring-boot:run
   ```

4. **启动前端**：
   ```bash
   cd frontend
   npm install
   npm run dev
   ```

---

## 八、功能演示

### 8.1 核心功能流程

1. **用户注册/登录** → 获取 JWT 令牌
2. **首页推荐** → 浏览推荐视频（按点赞数排序）
3. **视频互动** → 点赞、收藏视频
4. **发布视频** → 上传视频到 MinIO
5. **我的视频** → 管理个人视频（查看、删除）
6. **浏览记录** → 查看历史记录

---

## 九、总结

### 9.1 已完成分值统计

| 类别 | 总分 | 已完成 |
|------|------|--------|
| 视频推荐主页 | 20分 | ✅ 20分 |
| 我的视频管理 | 15分 | ✅ 15分 |
| 用户系统 | 5分 | ✅ 5分 |
| 后台功能设计 | 25分 | ✅ 25分 |
| **合计** | **70分** | **✅ 70分** |

### 9.2 超额完成内容

1. **收藏功能**：完整实现视频收藏功能
2. **浏览记录**：记录并展示用户浏览历史
3. **前端美化**：现代化 UI 设计，蓝青渐变主题
4. **性能优化**：React.memo、懒加载等优化策略
5. **监控支持**：Prometheus + Grafana 配置
6. **MinIO 集成**：对象存储服务

### 9.3 待优化项

1. 推荐算法可进一步优化（当前仅按点赞数排序）
2. 可增加视频评论功能
3. 可增加关注/粉丝系统
4. 可增加视频分享功能

---

**报告日期**：2026年6月14日  
**团队成员**：项目开发小组